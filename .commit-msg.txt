feat(networking): implement E05 dual VPC per environment

- Create reusable VPC module (infra/modules/vpc)
- Add dev/staging/prod Terraform roots with Control + Execution VPCs
- CIDR plan: non-overlapping /20 blocks across 6 VPCs
- 3-AZ subnets per VPC (public/private/data tiers)
- VPC peering between Control and Execution per environment
- 10 VPC endpoints (S3 GW + 9 Interface in Control VPCs)
- RFC1918 egress denial NACLs on Execution private subnets
- Least-privilege security group matrices (ALB/ECS/RDS)
- VPC flow logs to per-env S3 with KMS encryption
- NAT strategy: single (dev/staging), per-AZ HA (prod)
- Full CIDR plan documentation and E05 build journal
