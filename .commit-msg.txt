chore(networking): disable paid resources for cost optimization

Disable all resources that incur idle costs until workloads are deployed:
- NAT Gateways: set to 0 in all environments (was $259/mo)
- Interface Endpoints: commented out in all environments (was $216/mo)
- Flow Logs + KMS keys: commented out in all environments (was $3/mo)

Remaining cost: $0/mo (VPCs, subnets, route tables, peering, SGs, NACLs,
IGWs, and S3 Gateway endpoints are all free).

All paid resources are preserved as commented code with clear re-enable
instructions for when workloads are deployed.
